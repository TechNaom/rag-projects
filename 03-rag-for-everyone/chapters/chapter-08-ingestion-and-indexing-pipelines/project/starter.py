RUNS = []


def audit_run(run):
    # TODO: return publish decision, first issue, next action, and first metric
    return {}


if __name__ == "__main__":
    for run in RUNS:
        print(audit_run(run))
