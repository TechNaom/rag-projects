# Practice: Release Readiness Checklist

Use `starter.py` to check whether a deployment can move beyond demo status.

The solution requires auth, managed secrets, health checks, tracing, eval gates, and rollback. In production, "it runs" is the start of the checklist, not the end.
