# Diagrams: Deployment Patterns

- `deployment-reference.txt` shows the online answer path and the separate batch indexing path.
- `release-flow.txt` shows the minimum production release sequence for a RAG feature.
