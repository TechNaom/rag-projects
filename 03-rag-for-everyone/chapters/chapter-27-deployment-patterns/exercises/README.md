# Exercises: Deployment Patterns

Implement `recommend_pattern` in `starter.py`.

Run:

```bash
python3 starter.py
python3 solution.py
```

The recommended pattern should change when sensitivity, traffic, or latency expectations increase.
