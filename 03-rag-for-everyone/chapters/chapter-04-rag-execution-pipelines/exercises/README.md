# Exercises: RAG Execution Pipelines

Build a traceable pipeline model. The goal is not to call a real vector store yet. The goal is to learn what production traces must capture.

Run:

```bash
python3 starter.py
python3 solution.py
```
