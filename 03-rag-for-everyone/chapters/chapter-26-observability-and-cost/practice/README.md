# Practice: Cost Guardrails

Use `starter.py` to compare a baseline route with the current route.

The solution blocks a release when cost and latency regress together or when quality drops beyond tolerance. This is the production habit: do not save money by silently making the answer worse.
